# Chapter 3. 형식 언어와 정규 표현

---


## 1. 개념화 : 정규 표현 · 정규 문법 · 유한 오토마타

### 왜 정규 표현이 필요한가?

토큰(Token)은 **type : value** 의 1:n 대응 구조다.

```
integer-type : 9, 23, 345, +3, -3, 0000345, ...
```

이 모든 값을 일일이 나열할 수 없으니 **패턴(pattern)** 으로 표현한다.

```
(+|-)? [1-9] [0-9]*
```

> **패턴 → 규칙 → 정규 표현**

---

### 정규 표현 = 정규 문법

정규 표현은 **Well-formed formula** (잘 정의된 수식)다.

```
(+|-)? [1-9] [0-9]*   ← 정규 표현
A → a A | b            ← 정규 문법
```

> **정규 표현 → 형식(form) → 정규 문법**

ac 언어 문법 예시:

```
1  Prog  → Dcls Stmts $
2  Dcls  → Dcl Dcls
3         | λ
4  Dcl   → floatdcl id
5         | intdcl id
6  Stmts → Stmt Stmts
7         | λ
8  Stmt  → id assign Val Expr
9         | print id
10 Expr  → plus Val Expr
11        | minus Val Expr
12        | λ
13 Val   → id
14        | inum
15        | fnum
```

---

### 오토마타 (Automata)

> 정규 표현을 **구현하는 장치** (hypothetical machine)

- 입력에 따라 **상태(state)** 가 바뀜 (transition)
- **Finite Automata** : 상태 개수가 **유한(finite)**
  - 상태 수가 무한하면 동작 예측 불가 → Brownian motion
  - 입력 발생 시 (유한한 상태 중에서) 다음 상태를 선택

```
한글 오토마타 예시:
'ㅎ' (초성) → 'ㅏ' (중성) → 'ㄴ' (종성) → '한'
```

---

### 핵심 삼각 관계 (Trinitas)

```
     정규 표현
    (Regular Expression)
         ▲
        / \
       /   \
      /     \
정규 문법   유한 오토마타
(Regular   (Finite
 Grammar)   Automata)
```

> 세 가지는 **서로 동치(equivalent)** — 같은 표현력을 가진다.

---

## 2. 알파벳과 문자열

### 알파벳 (Alphabet, Σ)

> 기호(Symbol)들의 **유한 집합**

```
Σ = { a, b, c, ..., z }
Σ = { 0, 1 }
```

- 집합이므로 중복 원소 없음
- 원소 나열 순서 무관

---

### 문자열 (String)

> 주어진 알파벳 Σ에서 문자들을 **순서대로** 나열한 것

```
Σ = { a, b, c } 일 때
문자열 : a, ca, ccba, aabbcc, ...

단, abc ≠ cba  (순서가 다르면 다른 문자열)
    abb ≠ ab   (길이가 다르면 다른 문자열)
```

---

### 문자열 길이

> 문자열 `w`에 포함된 문자 개수 : **|w|** 로 표시

```
w = v₁v₂v₃ ··· vₖ  일 때  |w| = k

예: w₁ = abc → |w₁| = 3
    w₂ = abab → |w₂| = 4
```

---

### 빈 문자열 (Empty String, ε)

> 길이가 0인 문자열 : **ε** (엡사일론, 또는 λ 람다로도 표기)

```
|ε| = 0
```

**ε 연산:**

```
u · ε = u = ε · u
u · ε · v = u · v
```

**문자열의 n제곱:**

```
a¹ = a
a² = aa
aⁿ = aa···a  (n번 반복)
a⁰ = ε       (0번 = 빈 문자열)
```

---

### 문자열 연결 (Concatenation)

> 두 개 이상의 문자열을 이어붙여 하나로 만듦

```
u = a₁a₂···aₙ,  v = b₁b₂···bₘ  일 때
u · v = uv = a₁a₂···aₙb₁b₂···bₘ

|uv| = m + n
```

**⚠️ 교환 법칙 성립하지 않음:**

```
u = dog,  v = house 일 때
u · v = doghouse
v · u = housedog
uv ≠ vu
```

**접두사(prefix) / 접미사(suffix):**

```
w = uv 일 때,  u = 접두사,  v = 접미사

예: w = house
접두사 : h, ho, hou, hous, house
접미사 : e, se, use, ouse, house
```

---

## 3. 언어 (Language)

> **주어진 알파벳으로 만든 문자열들의 집합 (부분집합)**

### 예시

**Σ = {0, 1} 일 때:**

```
1. {0, 10, 1011}                             ← 유한 집합
2. {ε, 0, 00, 000, 0000, ...}               ← 0이 n개인 모든 문자열
3. 1의 개수가 짝수인 문자열
   {11, 0101, 1001, 10101010, ...}
```

**Σ = {키보드 문자 전체} 일 때:**

```
1. {0, +123, -123.456e+10}                  ← 숫자 표현
2. {if, for, while, case, switch, ...}      ← 키워드
3. {인천대학교, 정보기술대학, ...}            ← 한국어
```

---

## 4. 언어 연산

> 언어는 집합이므로 **집합 연산**을 사용

### Union (합집합, ∪)

```
L ∪ M = { s | s ∈ L 또는 s ∈ M }

예: L = {a, ba, bbb},  M = {aaa, bbb, aba, bba} 일 때
    L ∪ M = {a, ba, bbb, aaa, aba, bba}
    (bbb는 중복이므로 1번만)
```

---

### Concatenation (연결, ·)

```
L · M = LM = { s · t | s ∈ L 그리고 t ∈ M }

예: L = {aa, bb},  M = {ab, ba} 일 때
    L · M = {aaab, aaba, bbab, bbba}
    (L의 각 원소 뒤에 M의 각 원소를 붙임)
```

---

### 거듭제곱 (Lⁿ)

```
L⁰ = {ε}          ← 원소 1개 (공집합이 아님!)
L¹ = L · L⁰ = L
L² = L · L¹
Lⁿ = L · Lⁿ⁻¹    (n ≥ 1)
```

**예: L = {a, ba} 일 때:**

```
L⁰ = {ε}                                      원소 수: 2⁰ = 1
L¹ = {a, ba}                                   원소 수: 2¹ = 2
L² = {aa, aba, baa, baba}                      원소 수: 2² = 4
L³ = {aaa, aaba, abaa, ababa,
       baaa, baaba, babaa, bababa}              원소 수: 2³ = 8
```

> **|Lⁿ| = 2ⁿ** (L이 원소 2개일 때)

---

### Kleene Closure (L*)와 Positive Closure (L⁺)

**Kleene Closure (L*):**

```
L* = L⁰ ∪ L¹ ∪ L² ∪ ··· = ∪ Lⁱ  (i = 0 ~ ∞)
```

- **ε 포함** (L⁰ = {ε})
- L에 속하는 문자열을 0번 이상 반복해서 만들 수 있는 모든 문자열

**Positive Closure (L⁺):**

```
L⁺ = L¹ ∪ L² ∪ ··· = L* - L⁰ = L* - {ε}
```

- **ε 제외**
- 반드시 1번 이상 발생

**예: L = {0, 1} 일 때:**

```
L* : ε 포함, 0과 1로 만들어지는 모든 문자열
L⁺ : ε 제외, 0과 1로 만들어지는 모든 문자열 (반드시 1번 이상)
```

---

## 5. 정규 표현 (Regular Expression)

> **정규 언어(Regular Language)** : 정규 표현에 의해 정의되는 언어

### 정규 표현의 형식적 정의

알파벳 Σ에 대해:

1. `a ∈ Σ` 이면 `a`는 정규 표현
2. `Φ`(공집합)와 `ε`도 정규 표현
3. `r`, `s`가 각각 정규 언어 `Lᵣ`, `Lₛ`를 정의하는 정규 표현일 때:
   - `r | s` 는 `Lᵣ ∪ Lₛ`를 나타내는 정규 표현 (선택)
   - `r · s` 는 `Lᵣ · Lₛ`를 나타내는 정규 표현 (연결)
   - `r*`는 `(Lᵣ)*`를 나타내는 정규 표현 (반복)

> **Quiz:** `{}` 와 `{ε}` 은 같은가 다른가?  
> → 다르다. `{}` = 공집합(원소 없음), `{ε}` = ε 하나를 원소로 가진 집합

---

## 6. 정규 표현 연산

### 선택 (Choice, `|`)

```
r | s  →  Lᵣ ∪ Lₛ  (둘 중 하나)

예: Σ = {a, b}
a | b  →  L(a|b) = {a} ∪ {b} = {a, b}
a | ε  →  L(a|ε) = {a, ε}

다중 선택:
L(a|b|c|d) = {a, b, c, d}
```

---

### 연결 (Concatenation, `·`)

```
r · s  →  Lᵣ · Lₛ  (이어붙이기)
(· 생략 가능, rs로 사용)

예: Σ = {a, b, c}
ab  →  L(ab) = {a}{b} = {ab}

(a|b)c  →  L(a|b) · L(c) = {a,b} · {c} = {ac, bc}
a(b|c)  →  L(a) · L(b|c) = {a} · {b,c} = {ab, ac}

다중 연결:
L(a·b·c·d) = L(abcd) = {abcd}  ← 원소 1개
```

---

### 반복 (Repetition, `*`)

```
r*  →  (Lᵣ)*  (0번 이상 반복, ε 포함)

예: (a|bb)* 가 생성하는 언어

(a|bb)⁰ = {ε}
(a|bb)¹ = {a, bb}
(a|bb)² = {aa, abb, bba, bbbb}
(a|bb)³ = {aaa, aabb, abba, abbbb, ...}
...

→ a 또는 bb로 이루어진 모든 문자열 (ε 포함)
```

---

### 우선 순위

> **반복(`*`) > 연결(`·`) > 선택(`|`)**

```
a | bc*  →  a | (b · (c*))       ← *가 c에만 적용
ab | c*d →  ab | (c* · d)        ← *가 c에만 적용

우선순위 바꾸려면 괄호 사용:
a | bc  ≠  (a|b)c
```

---

## 7. 정규 정의 (Regular Definition)

> 복잡한 정규 표현에 **이름을 붙여** 재사용하는 방식

**예시 — 숫자열 정의:**

```
# 이름 없이 직접 표현
(0|1|2|...|9)(0|1|2|...|9)*

# 정규 정의 사용
digit = 0 | 1 | 2 | ... | 9
digit digit*  또는  digit+
```

> 정규 정의가 **읽기 쉽고, 확장하기 쉽고, 재사용하기 편함**

---

## 8. 정규 표현 확장 메타 기호

| 메타 기호 | 의미 | 예시 |
|----------|------|------|
| `r+` | 1번 이상 반복 (ε 제외) | `(0\|1)+` = 0과 1로 된 비어있지 않은 문자열 |
| `.` | 임의의 문자 1개 | `.*b.*` = b를 하나 이상 포함하는 모든 문자열 |
| `[a-z]` | 문자 범위 | `[a-z]` = a부터 z까지 소문자 |
| `[A-Za-z]` | 범위 결합 | 알파벳 대소문자 전체 |
| `~(abc)` | 부정 (not in set) | a, b, c를 제외한 임의의 문자 |
| `[^abc]` | 부정 (Lex 표기) | a, b, c를 제외한 임의의 문자 |
| `r?` | 선택적 (0 또는 1번) | `(+\|-)? nat` = 부호가 있거나 없는 자연수 |

**`+` vs `*` 비교:**

```
r*  →  0번 이상 (ε 포함)
r+  →  1번 이상 (ε 제외)
r+  =  r · r*
```

**`?` 활용 예시:**

```
nat = [0-9]+
signedNat = nat | +nat | -nat
          = (+|-)? nat      ← ? 로 간결하게 표현
```

---

## 9. 프로그래밍 언어 토큰 정규 표현

### 숫자 (Numbers)

```
nat        = [0-9]+
signedNat  = (+|-)? nat
number     = signedNat ("." nat)? (E signedNat)?
```

> `"."` : 메타 기호가 아닌 **실제 소수점 문자**

```
예: +3, -123, 3.14, 1.5E+10, -2.0E-3
```

### 예약어 (Reserved Words)

```
reserved = if | while | do | ...
```

### 식별자 (Identifiers)

```
letter     = [a-zA-Z]
digit      = [0-9]
identifier = letter (letter | digit)*
```

```
예: sum, user1, myVariable  ← 가능
    2user                   ← 불가능 (숫자로 시작)
    i                       ← 키워드이므로 불가능
```

---

## 10. Scanning 모호성 해결

> 하나의 문자열이 여러 정규 표현에 매칭될 수 있는 문제

### 문제 1 — 키워드 vs 식별자

```
if  →  키워드일 수도, 식별자일 수도 있음
```

**해결:** 키워드 해석을 우선 적용 (keyword interpretation preferred)

```
If I were a bird, I can fly.
Reserved = "If" | "I"
ID = letter (letter | digit)*

→ If : 키워드로 인식 (Reserved가 우선)
```

---

### 문제 2 — 긴 토큰 vs 짧은 토큰

```
<>  →  2개의 토큰 (<, >) 이거나 1개의 토큰 (<>) 일 수 있음
```

**해결:** **최장 일치 원칙 (Longest Substring)**  
→ 가장 길게 매칭되는 토큰 정의를 먼저 적용

```
relational_op = "<>" | ">" | "<"

If (a <> b) a = 100;
→ <> 를 토큰 1개로 인식 (더 긴 매칭 우선)
```

---

## 11. 주석 처리

> 주석(Comment) = 코드 생성과 무관 → **어휘 분석 단계에서 무시**

### 1개의 구분기호로 둘러싸인 경우

```
{ This is a Pascal comment }
정규 표현: {(~})*}
→ { 로 시작, } 가 나올 때까지 } 아닌 문자 반복, } 로 끝
```

### 특정 문자로 시작 ~ 줄 끝까지

```
; This is a Scheme comment
정규 표현: ;(~newline)*

-- This is an Ada comment
정규 표현: --(~newline)*
```

### 2개의 구분기호로 둘러싸인 경우

```
/* This is a C comment */
형태: ba ... (ab가 등장하지 않음) ... ab

정규 표현: ba(~(ab))*ab
→ ba로 시작, ab가 아닌 문자 반복, ab로 끝
```

---

## 12. 실습 문제 정리

### Practice #1 — `(a|bb)*` 가 생성할 수 **없는** 문자열 찾기

> `(a|bb)*` = a 또는 bb로 이루어진 모든 문자열

```
✅ a              → a 1번
✅ aaaaaaaaaaaaa  → a 13번
✅ abbaaabbbba    → a, bb, a, a, a, bb, bb, a, a → 가능
✅ abbabaaaa      → a, bb, a, b, a, a, a, a → b 1개 단독 등장 → 불가능?
✅ bbbbabb        → bb, bb, a, bb → 가능
❌ bbba           → bbb는 bb+b인데 b가 단독으로 남음 → 불가능
```

> **핵심:** `b` 단독으로는 생성 불가. 반드시 `bb` 쌍으로 등장해야 함.

---

### Practice #2 — 정규 표현이 생성하는 언어

**1번:**
```
(aa|b)(a|bb)

L(aa|b) = {aa, b}
L(a|bb) = {a, bb}
→ {aaa, aabb, ba, bbb}
```

**2번:**
```
a*(a|b)
→ a를 0번 이상 반복한 뒤 a 또는 b

= {a, b, aa, ab, aaa, aab, aaaa, aaab, ...}
= b로 끝나거나 a로만 이루어진 문자열
```

**같은 언어인지 비교:**

```
1. (a|bb)* vs a|bb*
   → 다르다
   (a|bb)* : a 또는 bb의 반복 (ε 포함)
   a|bb*   : a 하나, 또는 b 뒤에 b가 0번 이상 = {a, b, bb, bbb, ...}

2. a(ba)* vs (ab)*a
   → 같다
   둘 다 : a로 시작하고 a로 끝나는, ab가 반복되는 문자열
   = {a, aba, ababa, abababa, ...}
```

---

### Practice #3 — 정규 표현의 언어 특징

```
1. (0|1)(0|1)*
   → 0 또는 1로 시작하는 0, 1 문자열
   = 0과 1로 이루어진 모든 이진 문자열 (길이 ≥ 1)
   = (0|1)+

2. (A|B|...|Z)(a|b|...|z)*
   → 대문자로 시작, 소문자가 0번 이상 반복
   = 첫 글자가 대문자인 식별자 (ex: Name, Hello)

3. (+|-)(1|2|...|9)(0|1|...|9)*
   → 부호 필수, 0이 아닌 숫자로 시작하는 정수
   = 부호 있는 0이 아닌 정수 (ex: +3, -123)

4. (0x|0X)(0|1|...|9|A|B|C|D|E|F)*
   → 0x 또는 0X로 시작하는 16진수 리터럴
   = C언어 16진수 표현 (ex: 0x1F, 0XA3)
```

---

### Practice #4 — 특정 조건 문자열 정의

**Σ = {a, b, c} 일 때:**

```
# 정확히 b 1개만 포함하는 문자열
(a|c)* b (a|c)*
→ b 앞뒤로 a나 c가 0번 이상, b는 정확히 1번

# 최대 b 1개만 포함 (0개 또는 1개)
(a|c)* | (a|c)* b (a|c)*
또는
(a|c)* (b|ε) (a|c)*
→ b가 없거나, b가 1개
```

---

### Practice #5 — 정규 표현의 한계

**Σ = {a, b} 일 때,  aⁿbaⁿ (n ≥ 1) 형태의 문자열:**

```
L = {aba, aabaa, aabaaa, ...} = {aⁿbaⁿ | n ≠ 0}
```

**답: `a*ba*` 로는 표현 불가!**

> `a*ba*` 는 b 양쪽 a의 **개수가 같아야 한다는 조건을 표현할 수 없다.**

**이유:**

```
regular expression can't count.
(정규 표현은 개수를 셀 수 없다)
```

- **유한 오토마타는 메모리가 없어** 이전 상태를 기억하지 못함
- 단어 찾기 → 상태 기억 불필요 → 정규 표현으로 가능
- 문장 구조 찾기 → 상태 기억 필요 → CFG/푸시다운 오토마타 필요

> **regular set** : 정규 표현으로 나타낼 수 있는 집합  
> **non-regular set** : `{aⁿbaⁿ}` 처럼 정규 표현으로 나타낼 수 없는 집합

---

### Practice #6 — 복잡한 정규 표현 해석

```
((b|c)* a (b|c)* a)* (b|c)*

(b|c)* a (b|c)* a  →  a를 정확히 2개 포함하는 부분

b|c 를 nota(a가 아닌 것) 로 표현하면:
nota* a nota* a  →  a가 정확히 2개

전체:
(nota* a nota* a)* nota*
→ a의 개수가 짝수(even)인 문자열 (0개 포함)
```

---

### Practice #7 — 집합을 정규 표현으로

```
L₁ = {0, 1, 2}           →  0 | 1 | 2
L₂ = {ε, ab}              →  ε | ab
L₃ = {a, b, aab, bba}    →  a | b | aab | bba
L₄ = {ε, 0, 00, 000, ...} →  0*
```

---

### Practice #8 — 문자열 길이 조건

**Σ = {a, b} 일 때:**

```
# 길이가 정확히 2
{aa, ab, ba, bb}
→ (a|b)(a|b)

# 길이가 최소 2 이상
{aa, ab, ba, bb, aaa, ...}
→ (a|b)(a|b)(a|b)*

# 길이가 최대 2 이하 (0, 1, 2)
{ε, a, b, aa, ab, ba, bb}
→ ε | (a|b) | (a|b)(a|b)
→ ε | (a|b)(ε|(a|b))
```

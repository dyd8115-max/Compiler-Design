# 컴파일러 — Chapter 2 언어 번역기 종류

---

## 1. 형식 언어 4가지 타입

언어의 종류:
- **자연어 (natural language)** : 인간이 사용하는 언어
- **형식 언어 (formal language)** : 수학적으로 정의된 언어

Chomsky 분류에 따른 형식 언어 4종류:

```
┌─────────────────────────────────────────────────┐
│  재귀 열거 언어 (recursively enumerable language) │
│  ┌─────────────────────────────────────────────┐ │
│  │  문맥 인식 언어 (context-sensitive language) │ │
│  │  ┌──────────────────────────────────────┐   │ │
│  │  │  문맥 자유 언어 (context-free language) │   │ │  ← 컴파일러가 사용
│  │  │  ┌──────────────────────────────┐    │   │ │
│  │  │  │  정규 언어 (regular language) │    │   │ │  ← 스캐너가 사용
│  │  │  └──────────────────────────────┘    │   │ │
│  │  └──────────────────────────────────────┘   │ │
│  └─────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

**형식 언어 4종류 (안쪽이 더 제한적)**

- **정규 언어 (Type-3)** : 가장 단순한 언어. 토큰(단어) 패턴 정의에 사용
- **문맥 자유 언어 (Type-2)** : 프로그래밍 언어의 문장 구조 정의에 사용. 컴파일러의 파서가 담당
- **문맥 인식 언어 (Type-1)** : 문맥에 따라 의미가 달라지는 언어
- **재귀 열거 언어 (Type-0)** : 가장 복잡한 언어. 튜링 머신으로만 인식 가능

**왜 다른 타입의 언어가 필요한가?**

- **정규 언어 (Type-3)** → 단어(토큰) 패턴 정의에 사용
  - 영어 단어는 어떻게 만들까?
  - PL에서 변수 이름은 어떻게 정의할까?
  - 정규 표현식(Regular Expression)으로 표현
- **문맥 자유 언어 (Type-2)** → 문장 구조 정의에 사용
  - 영어 3형식 문장 : S + V + O
  - PL에서 if 문 : `if (조건식) then 문장 else 문장`
  - 문맥 자유 문법(Context Free Grammar)으로 표현

---

## 2. 번역기 종류 개요

```
소스 프로그램 ──→ 번역기 ──→ 목적 프로그램
```

| 입력 | 번역기 | 출력 |
|------|--------|------|
| 어셈블리어로 작성된 프로그램 | 어셈블러 (Assembler) | 기계어 프로그램 |
| 고급 언어로 작성된 프로그램 | 컴파일러 (Compiler) | 어셈블리어 또는 기계어 |
| 고급 언어로 작성된 프로그램 | 전처리기 (Preprocessor) | 고급 언어로 작성된 프로그램 |
| 고급 언어로 작성된 프로그램 | 인터프리터 (Interpreter) | 실행 결과 |

---

## 3. 어셈블러 (Assembler)

어셈블리어를 기계어로 번역하는 번역기.

> **주요 어셈블리 언어 예시**: x86 Assembly, ARM Assembly, MIPS Assembly, NASM, MASM

**어셈블리어 (Assembly Language)**
- 이진 코드(기계어)를 사람이 이해하기 쉽게 기호화(mnemonics)한 언어
- 어셈블러가 아래와 같이 어셈블리어를 기계어로 번역함

| 어셈블리어 | 기계어 |
|-----------|--------|
| `mov al, 061h` | `10110000 01100001` |

**어셈블리 예시**

```asm
LOAD R1, a    -- 변수 a의 값을 레지스터 R1에 저장 (R1 ← MEM(a))
ADD R1, #2    -- R1의 값과 숫자 상수 2를 더해 다시 R1에 저장 (R1 ← R1 + 2)
STORE b, R1   -- R1에 저장된 값을 변수 b에 저장 (MEM(b) ← R1)

-- b = a + 2 와 동일
-- 변수 a, b는 메인 메모리 주소에 저장됨
```

### 투 패스 어셈블러 (Two-pass Assembler)

대부분의 어셈블러는 소스 프로그램을 두 번 처리함.

> **패스 (pass)** : 소스 프로그램을 처리하는 세부 작업

**왜 두 번 나눠서 처리할까?**

어셈블리어 코드를 위에서 아래로 한 번만 읽으면서 기계어를 생성하면, 아직 등장하지 않은 변수나 라벨을 참조할 때 주소를 알 수 없는 문제가 생김. 이를 **전방 참조(forward reference)** 문제라고 함.

```asm
LOAD R1, a      -- a가 아직 어디 있는지 모름!
ADD R1, #2
STORE b, R1     -- b도 아직 어디 있는지 모름!
```

이 문제를 해결하기 위해 1차 패스에서 먼저 주소 정보를 수집하고, 2차 패스에서 그 정보를 활용해 기계어를 생성함.

**1차 패스 (First Pass)**
- 소스 코드를 처음부터 끝까지 읽으며 **식별자(identifier)가 메모리 어느 주소에 위치하는지** 수집해 심볼 테이블에 저장
- 아직 기계어로 변환하지 않음. 주소 지도만 만드는 단계

| 식별자 이름 | 주소 |
|------------|------|
| a | 0 |
| b | 4 |

> **식별자(identifier)** : 프로그램에서 변수, 함수, 라벨 등 각종 요소에 붙이는 이름. 컴파일러/어셈블러는 이 이름들을 실제 메모리 주소로 변환함

**2차 패스 (Second Pass)**
- 1차 패스에서 만든 심볼 테이블을 참조해 **실제 기계어 코드를 생성**
- 연산 코드(op code) + 레지스터 지정 + 주소 지정 방식 결정
- 변수 이름을 심볼 테이블에서 찾아 실제 주소로 치환
  - 예: `LOAD R1, a` → 심볼 테이블에서 a의 주소 `0` 확인 → `00000000` 대입

> **레지스터(register)** : CPU 내부에 있는 임시 저장 공간. 메모리(RAM)보다 훨씬 빠르지만 개수가 적음. 연산을 하려면 메모리에서 값을 레지스터로 가져와서 처리하고(`LOAD`) 결과를 다시 메모리에 저장(`STORE`)하는 흐름으로 동작

```
0001 01 00 00000000*   ←→   LOAD R1, a
0011 01 10 00000010    ←→   ADD R1, #2
0010 01 00 00000100*   ←→   STORE b, R1
```

기계어 비트 구조:
- **Bits 0~3** : 연산 코드(op code). 명령어 종류 결정. 예: `0001` = LOAD, `0011` = ADD, `0010` = STORE
- **Bits 4~5** : 레지스터 번호. 2비트이므로 총 4개(`00`~`11`) 표현 가능. 예: `01` = 레지스터 1(R1)
- **Bits 6~7** : 주소 지정 방식(addressing mode). 피연산자를 어떻게 가져올지 결정
  - `00` : 직접 주소 지정(direct addressing) — Bits 8~15가 피연산자가 저장된 메모리 주소를 가리킴. 예: `LOAD R1, a` → 주소 `00000000`에서 값 가져옴
  - `10` : 즉시 주소 지정(immediate addressing) — Bits 8~15 자체가 피연산자 값. 메모리를 거치지 않고 명령어 안에 값이 직접 들어있음. 예: `ADD R1, #2` → `00000010` 자체가 값 2
- **`*`** : 절대 주소가 아니라 상대 주소(relative address)임을 표시. 링커가 최종 메모리 주소를 결정하기 전 단계이므로 아직 확정되지 않은 주소임을 의미. 예: `STORE b, R1` → b의 주소 `00000100*`

---

## 4. 전처리기 (Preprocessor)

프로그래밍 언어의 기능을 확장시켜 주는 번역기.

- 원시 언어와 목적 언어가 모두 고급 언어

> **전처리기를 사용하는 언어 예시**: C, C++ (`#include`, `#define`), Objective-C, Fortran (조건부 컴파일)

**C 전처리기 예시**

1) **파일 포함 지시어** : 헤더 파일을 포함
```c
#include <global.h>   // 해당 코드를 <global.h> 파일 내용으로 통째로 바꿈
```

2) **매크로 (macro)** : 매크로로 정의한 내용을 확장
```c
#define max 45   // 프로그램 전체에서 max를 모두 45로 바꿈
```

3) **조건부 컴파일** : 조건에 따라 선택적으로 코드를 포함
```c
#if SYSTEM == WINDOWS
    #include "stdio.h"
#elif SYSTEM == UNIX
    #include "unix.h"
#else
    #include "etc.h"
#endif
```

> **C의 `#include` vs Java의 `import`** : C의 `#include`는 전처리기가 해당 파일 내용을 통째로 복붙하는 텍스트 치환이지만, Java의 `import`는 전처리기 없이 컴파일러가 직접 처리하며 어느 클래스를 쓸지 알려주는 것일 뿐 파일 내용을 복붙하지 않음. 따라서 Java에는 전처리기가 없음

---

## 5. 컴파일러 (Compiler)

**컴파일러(Compiler)** : 고급 언어로 작성된 소스 프로그램 전체를 실행 전에 기계어로 번역하는 번역기. 번역 결과물인 목적 프로그램은 이후 링커/로더를 거쳐 실행됨

> **컴파일러를 사용하는 언어 예시**: C, C++, Go, Rust, Swift, Kotlin, Fortran, Pascal  
> (소스 → 기계어/어셈블리어로 미리 전체 번역)

### 컴파일러 vs 인터프리터

| 구분 | 컴파일러 | 인터프리터 |
|------|----------|-----------|
| 번역 시점 | 실행 전에 전체 번역 | 실행 시점에 줄 단위 번역 |
| 실행 속도 | 빠름 | 느림 |
| 메모리 사용 | 많음 (기계어로 변환된 코드 저장) | 적음 |
| 재실행 | 번역 없이 목적 코드 재사용 | 매번 다시 번역 |
| 유리한 경우 | 반복문, 자주 호출되는 서브루틴 | 스크립트, 대화형 실행 |

**컴파일 방식 실행 흐름**

```
고급 언어로 작성된 소스 프로그램
       ↓
    컴파일러 (RAM에서 실행 ← 컴파일러 자체가 RAM에 올라가 작업)
      ① 파싱/디코드 : 소스코드 텍스트 읽고 문법 구조 파악 + 오류 검출
      ② AST 생성 : 파악한 구조를 트리로 표현
      ③ 최적화 : 불필요한 코드 제거 등
      ④ 인코드 : 기계어로 변환 (CPU가 바로 실행할 수 있는 형태로)
       ↓
    목적 프로그램 (기계어, 디스크에 저장)
       ↓
   링커 (기계어 파일들을 하나로 합침, 디코드 없음)
       ↓
   로더 (목적 프로그램을 실행용으로 RAM에 올림 ← 컴파일러와 별개로 다시 올림)
       ↓
   CPU (fetch → decode → execute)
       ↓
      결과
```

> 컴파일러에서 파싱/디코드를 수행하는 이유:
> - **오류 조기 발견** : 실행 전에 문법 오류, 타입 불일치 등을 미리 잡아냄
> - **CPU를 위한 준비** : 고급 언어를 CPU가 바로 실행할 수 있는 기계어로 변환하기 위해 먼저 의미를 파악해야 함. 의미 파악(디코드) 없이는 기계어 변환(인코드) 불가

**반복문/서브루틴에 유리한 이유**
- 컴파일러는 미리 기계어로 변환해서 저장해두기 때문에 반복문에서 같은 코드를 만나도 저장된 기계어를 그대로 실행만 하면 됨
- 인터프리터는 같은 코드를 만날 때마다 매번 다시 번역해야 함

**메모리가 많이 필요한 이유**
- 몇 줄의 원시 프로그램이 몇 백 줄의 기계어로 번역되고 파일(.exe 등)로 디스크에 저장됨
- 실행 시 그 기계어 전체가 RAM에도 올라감
- 인터프리터는 한 줄 실행 후 버리기 때문에 저장 공간이 적게 필요

**링커(Linker)** : 여러 목적 파일과 라이브러리를 하나의 실행 파일로 합치는 도구

**로더(Loader)** : 링커가 만든 실행 파일을 메인 메모리에 올려 실행 가능한 상태로 만드는 도구

**컴파일 에러 vs 런타임 에러 vs 논리 에러**

| 구분 | 발생 시점 | 원인 | 예시 |
|------|----------|------|------|
| 컴파일 에러 (Compile Error) | 실행 전 컴파일 단계 | 문법 오류, 타입 불일치 | `int a = "hello"` |
| 런타임 에러 (Runtime Error) | 실행 중 — 프로그램이 강제 종료됨 | 코드는 문법적으로 맞지만 실행 중 불가능한 연산이 발생. 0으로 나누기, 메모리 초과, 배열 범위 초과 | `int arr[3]; arr[10] = 1;` |
| 논리 에러 (Logic Error) | 실행 중 — 프로그램이 종료되지 않고 계속 돌아감 | 코드는 문법적으로 맞고 실행도 되지만 의도와 다르게 동작 | 무한 루프, 잘못된 계산 결과 |

> 컴파일러 방식의 장점 중 하나가 실행 전에 컴파일 에러를 미리 잡을 수 있다는 것. 인터프리터는 실행해봐야 오류를 알 수 있음

> **AOT 컴파일(Ahead-Of-Time Compilation)** : 실행 전에 소스코드 전체를 미리 기계어로 변환해두는 방식. C, C++, Go, Rust 같은 전통적인 컴파일러 방식이 여기에 해당. 반대 개념은 실행 중에 그때그때 변환하는 JIT(Just-In-Time) 컴파일
>
> | | 변환 시점 | 예시 |
> |--|---------|------|
> | **AOT** | 실행 전에 미리 전체 변환 후 저장 | C, C++, Go, Rust |
> | **JIT** | 실행 중에 그때그때 변환, 자주 실행되는 코드는 기계어로 저장해두고 재사용 | JavaScript, Java(JVM) |
> | **인터프리터** | 실행 중에 한 줄씩 변환+실행 후 버림 | Python, Ruby |

---

## 6. 인터프리터 (Interpreter)

줄(셀) 단위로 번역하면서 즉시 실행.

> **인터프리터를 사용하는 언어 예시**: Python, Ruby, PHP, Perl, JavaScript (브라우저 환경), R, Bash/Shell script  
> (실행 시점에 한 줄씩 읽고 바로 실행)

**인터프리터 실행 흐름**

fetch 단계에서 가져오는 것은 인터프리터 방식에 따라 다름

**① 바이트코드 방식 (Python 등)**
- **바이트코드(bytecode)** : 소스코드를 완전한 기계어로 변환하기 전 단계의 중간 코드. 사람이 읽는 소스코드보다는 낮은 수준이지만 기계어보다는 높은 수준의 이진수 형태
- 실행 전에 소스코드를 바이트코드(이진수)로 미리 변환해둠
- Java도 바이트코드를 사용하지만 JVM이 실행하는 혼합형이라 순수 인터프리터는 아님

```
시작
  ↓
소스코드 (RAM에 올림)
  ↓ RAM에서 수행:
    ① 파싱/디코드 : 소스코드 텍스트 읽고 문법 구조 파악
    ② AST 생성 : 파악한 구조를 트리로 표현
    ③ 인코드 : 바이트코드(이진수)로 변환 후 저장 (.pyc 등)
  ↓
fetch : 이진수로 된 바이트코드를 CPU로 전송
  ↓
decode : 이진수를 해석해 어떤 명령인지 파악
  ↓
지정된 피연산자 호출
  ↓
지정된 연산으로 분기  ← 분기(jump, break, continue), 반복, 조건 명령일 때
  ↓
원시 연산 1 실행 ... 원시 연산 k 실행
  ↓
명령어 주소 하나 증가
  ↓ (반복)
```

**② 소스코드 직접 해석 방식 (순수 인터프리터)**
- 소스코드 텍스트를 한 줄씩 그대로 가져와서 바로 해석하고 실행

```
시작
  ↓
fetch : 소스코드 텍스트 한 줄을 CPU로 전송 (예: `"print(a)"` 를 메모리에서 CPU로 전송)
  ↓
decode : 텍스트 파싱 + 기계어 변환 + 어떤 명령인지 파악 동시에 실행 (예: print 명령이구나)
  ↓
지정된 피연산자 호출 (예: `print(a)` 에서 `a` 의 값을 메모리에서 읽어옴)
  ↓
지정된 연산으로 분기  ← 분기(jump, break, continue), 반복, 조건 명령일 때
  ↓
원시 연산 1 실행 ... 원시 연산 k 실행
  ↓
명령어 주소 하나 증가
  ↓ (반복)
```

**바이트코드 방식 vs 순수 인터프리터 비교**

단순히 한 번 실행하면 총 작업량은 비슷하지만, 반복 실행할 때 차이가 남

| | 바이트코드 방식 | 순수 인터프리터 |
|--|---------------|---------------|
| 파싱 횟수 | 최초 1번만 (바이트코드 변환 시) | 실행할 때마다 매번 |
| 반복문에서 같은 코드 100번 | 파싱 1번 + decode 100번 | 파싱+decode 100번 |
| 속도 | 반복 실행할수록 빠름 | 반복 실행할수록 느림 |

> - **인코드 (encode)** : 정보를 특정 형식(이진수 등)으로 변환
> - **디코드 (decode)** : 변환된 형식을 다시 원래 의미로 해석. 예: `0001 01 00 00000000` → `LOAD R1, a`

**주로 스크립트(script) 언어에서 사용**
- 스크립트 언어 : 다른 언어에 삽입할 수 있는 언어 (예: HTML + JavaScript)
- PHP, JSP, ASP, VBScript, Perl, Ruby, **Python** 등

---

## 7. 혼합형 컴파일러 (Compiler + Interpreter)

컴파일러와 인터프리터를 혼합한 방식.

> **혼합형을 사용하는 언어 예시**: Java (소스 → 바이트코드 → JVM 인터프리트), Kotlin (JVM 기반), Scala, Groovy, C# (소스 → IL → CLR 실행), Python (소스 → .pyc 바이트코드 → CPython 인터프리트)

**Java가 대표적인 예**
- 소스 프로그램을 바이트코드(bytecode)라 불리는 중간 코드로 컴파일
- 바이트코드는 JVM(Java Virtual Machine)에서 인터프리트 방식으로 실행

```
소스 프로그램
       ↓
    컴파일러
       ↓
  중간 언어 프로그램
  [  bytecode  ]
       ↓
    인터프리터
     [  JVM  ]
       ↓
     출력
  [ target code ]
```

> - 바이트코드 단계까지는 컴파일러 방식
> - 실행은 JVM이 인터프리트 방식으로 처리
> - Intel이든 ARM이든 JVM만 있으면 동일한 바이트코드 실행 가능 → 플랫폼 독립적

**혼합형의 이점**

| | 가져온 장점 | 내용 |
|--|-----------|------|
| **컴파일러 장점** | 빠른 실행 | 바이트코드로 미리 변환해두니까 순수 인터프리터보다 빠름 |
| | 오류 조기 발견 | 컴파일 단계에서 문법 오류 미리 잡을 수 있음 |
| **인터프리터 장점** | 플랫폼 독립성 | JVM만 있으면 Windows, Mac, Linux 어디서든 동일한 바이트코드 실행 가능 |

> **Write Once, Run Anywhere** : Java의 핵심 철학. C/C++은 플랫폼마다 따로 컴파일해야 하지만 Java는 바이트코드 하나로 JVM 있는 곳 어디서든 실행 가능. 서버 환경이 다양한 기업용 소프트웨어에서 Java가 많이 쓰이는 이유

---

## 8. 크로스 컴파일러 (Cross Compiler)

컴파일러를 실행하고 있는 컴퓨터의 기계어가 아닌 **다른 컴퓨터의 기계어**로 번역하는 컴파일러.

> **크로스 컴파일러 사용 예시**: GCC (PC에서 ARM용 코드 생성), LLVM/Clang, Android NDK (x86 PC → ARM Android), iOS 앱 개발 (Mac에서 ARM iPhone용 바이너리 생성)

```
기존 컴퓨터 (개발 환경)
  소스 프로그램
       ↓
  크로스 컴파일러
       ↓
  새 CPU용 기계어 코드
       ↓ (이 코드를 옮겨서)
새 컴퓨터 (목적 환경)
  실행
```

**필요한 경우**
- 최신 CPU를 장착할 새 컴퓨터 개발이 아직 끝나지 않았을 때
- 기존 컴퓨터에서 새 CPU용 기계어 코드를 미리 컴파일
- 개발 중인 새 컴퓨터에서 그 코드를 실행

> 임베디드 시스템 개발에서 자주 사용. 예: PC에서 ARM용 코드를 컴파일해서 스마트폰/IoT 기기에 올림

---

## 9. 언어 처리 시스템

소스 코드가 실행 가능한 목적 기계 코드가 되기까지의 전체 흐름.

```
문제
  ↓ 문제 분석
알고리즘 작성
  ↓ 언어 선택
소스 프로그램
  ↓
프리 프로세서 (전처리기, 파일 포함, 매크로)
  ↓
확장된 소스 프로그램
  ↓
컴파일러
  ↓
재배치 가능 기계 코드 (relocatable machine code)
  ↓               ↑ 링커가 링킹 ←── 프로시저 / 함수 / 모듈
하나의 재배치 가능 기계 코드
  ↓               ↑ 로더가 로딩
실행 가능한 목적 기계 코드 (absolute machine code)
```

> 위 흐름은 **C/C++ 기준** 예시. 언어마다 일부 단계가 다름

**언어별 언어 처리 시스템 단계 비교**

| | 전처리기 | 컴파일러 | 링커 | 로더 |
|--|---------|---------|------|------|
| **C/C++** | ✅ (#include, #define) | ✅ → 기계어 | ✅ | ✅ |
| **Java** | ❌ | ✅ → 바이트코드 | ✅ | ✅ (JVM) |
| **Python** | ❌ | ✅ → .pyc | ❌ | ❌ (인터프리터가 직접 실행) |

**재배치 가능 기계 코드 (relocatable machine code)**
- 컴파일러가 소스코드를 기계어로 변환한 결과물 (.o 파일)
- 메인 메모리 어디에 올릴지 아직 결정되지 않은 상태의 기계 코드
- 상대 주소(`*`)로 표시되어 있어 링커가 최종 주소를 확정해줘야 함
- 소스파일 하나당 하나씩 생성. 예: `main.c` → `main.o`

### 링커 (Linker)

- 여러 개의 재배치 가능 기계 코드(.o)를 하나로 합치는 역할
- 프로시저, 함수, 모듈 등 외부 코드를 연결. 예: 내 코드에서 `printf()` 호출 시 링커가 C 표준 라이브러리에서 `printf` 구현을 찾아서 연결
- 함수 호출 주소 연결 — 내 코드에서 외부 함수를 부르면 실제 위치를 링커가 연결해줌
- 상대 주소(`*`)를 실제 주소에 가깝게 정리 (로더가 최종 확정)

**절대 기계 코드 (absolute machine code)**
- 로더가 메인 메모리에 올린 후 최종 메모리 주소가 확정된 상태
- 메모리 공간과 주소가 결정됨
- 실행 준비 완료

### 로더 (Loader)

하나로 합쳐진 기계 코드를 메인 메모리에 올리는 역할.
- 메모리 주소를 확정하고 실행 가능한 상태로 만듦

---

## 10. CPU의 명령어 처리 과정

**CPU(Central Processing Unit)** : 컴퓨터의 두뇌. 기계어(이진수)로 된 명령어를 fetch → decode → execute 순으로 처리함

### fetch → decode → execute 흐름

```
메모리에 저장된 기계어
    ↓ fetch : CPU가 명령어를 메모리에서 가져옴
이진수 덩어리 (예: 0001 01 00 00000000)
    ↓ decode : 이진수를 잘라서 의미 파악
      - 앞 4비트(0001) → op code → LOAD
      - 다음 2비트(01)  → 레지스터 번호 → R1
      - 다음 2비트(00)  → 주소 지정 방식 → 직접 주소
      - 나머지(00000000) → 메모리 주소 → 0
    ↓ execute : 파악한 명령 실행
실행 완료 → 다음 명령어로 이동
```

> decode가 필요한 이유: 이진수를 받았다고 CPU가 의미를 자동으로 파악하는 게 아니라, 어디서 어디까지가 뭔지 잘라서 해석하는 과정이 반드시 필요함. 전화번호 `01012001234`를 `010-1200-1234`로 잘라야 의미를 알 수 있는 것과 같음

### 디코드(decode) 개념

디코드는 컴파일러/인터프리터와 CPU, 두 곳에서 일어나는데 맥락이 달라.

**① 컴파일러/인터프리터에서의 디코드 (파싱)**
- 소스코드 텍스트를 읽고 의미를 파악하는 과정. 컴파일러 용어로는 **파싱(parsing)** 이라고 부름
- 토큰으로 쪼개고 → 문법 구조 파악 → AST 생성 순서로 진행
- ch2-2에서 배운 Scanner → Parser → AST가 바로 이 디코드 과정
- **목적** : 소스코드가 문법적으로 맞는지 검증 + 의미 파악
- **얻는 것** : AST → 이후 기계어/바이트코드 생성에 사용. 의미 파악 없이는 기계어 변환 불가

```
"b = a + 3.2" 파싱 없이는
→ 이게 할당인지 비교인지 모름 → 기계어 변환 불가
```

**② CPU에서의 디코드**
- 기계어 이진수를 받아서 어떤 명령인지 파악하는 과정
- 비트를 정해진 규칙대로 잘라서 op code, 레지스터, 주소 지정 방식을 파악
- **목적** : 이진수 덩어리가 어떤 명령인지 파악
- **얻는 것** : op code, 레지스터, 주소 → 이걸 알아야 execute 가능

```
0001 01 00 00000000
 ↓    ↓  ↓    ↓
LOAD  R1 직접주소 0번지
```

**공통점 — 둘 다 본질은 같음**
- "뭔지 모르는 덩어리를 규칙에 따라 잘라서 의미를 파악하는 것"

| | 컴파일러/인터프리터 디코드 | CPU 디코드 |
|--|--------------------------|-----------|
| 대상 | 소스코드 텍스트 | 기계어 이진수 |
| 규칙 | 문법(Grammar) | 명령어 집합(ISA) |
| 결과 | AST, 기계어 | 실행할 연산 |

### 컴파일러 vs 인터프리터의 CPU 처리 차이

**컴파일러 방식**
- 실행 전에 소스코드를 파싱(디코드)해서 기계어로 변환 후 저장
- 실행 시 저장된 기계어를 CPU에 전달 → CPU가 fetch → decode → execute
- 소스코드 디코드는 컴파일 시점에 한 번만 하고, 이후 실행할 때는 CPU 디코드만 함

```
소스코드 파싱(디코드) → 기계어 저장
                            ↓ 실행 시
                        CPU fetch → decode → execute
```

**인터프리터 방식**
- 한 줄 읽고 → 디코드(어떤 명령인지 파악) → CPU에 전달 → CPU decode → execute → 버림
- 기계어를 저장하지 않고 바로 버리기 때문에 반복문에서 같은 줄을 만나면 매번 처음부터 디코드 반복
- 디코드가 실행 시점마다 일어나서 컴파일러보다 느림

```
한 줄 읽음 → 디코드 → CPU decode → execute → 버림
                                              ↓
                                         다음 줄 반복
```

> 컴파일러와 인터프리터 모두 CPU 디코드는 반드시 거침. 차이는 소스코드 디코드(파싱)를 **미리 해두느냐(컴파일러)** vs **실행할 때마다 하느냐(인터프리터)** 임

### 번역기별 디코드 위치와 타이밍

| | 소스코드 파싱 위치/타이밍 | 바이트코드 디코드 | CPU 디코드 |
|--|------------------------|----------------|-----------|
| **컴파일러(AOT)** | RAM / 컴파일 시점 1번만 | 없음 | 실행할 때마다 |
| **순수 인터프리터** | CPU / 실행할 때마다 (소스코드 그대로 CPU로 전송, RAM은 저장+전송만) | 없음 | 실행할 때마다 |
| **바이트코드 인터프리터(Python)** | RAM / 최초 1번 (.pyc 변환 시) | RAM / 실행할 때마다 | 실행할 때마다 |
| **혼합형(Java)** | RAM / 컴파일 시점 1번만 | JVM(RAM) / 실행할 때마다 | 실행할 때마다 |
| **JIT(JavaScript)** | RAM / 실행 시점 1번만 | 없음 | 실행할 때마다 |

> - 소스코드 파싱은 항상 RAM에서 수행. 소스코드를 디스크에서 읽어 RAM에 올린 뒤 파싱 진행
> - CPU 디코드는 모든 방식에서 실행할 때마다 반드시 발생
> - 차이는 소스코드 파싱을 **미리 하느냐(컴파일러, 혼합형)** vs **실행할 때마다 하느냐(순수 인터프리터)**

---

## 핵심 요약

```
번역기 종류
  어셈블러    : 어셈블리어 → 기계어
                예) x86 Assembly, ARM Assembly, MIPS Assembly
  컴파일러    : 고급 언어 → 기계어 (실행 전 전체 번역)
                예) C, C++, Go, Rust, Swift, Kotlin
  전처리기    : 고급 언어 → 고급 언어 (매크로, 파일 포함 등)
                예) C/C++ #include, #define
  인터프리터  : 고급 언어 → 실행 결과 (줄 단위 번역+실행)
                예) Python, Ruby, PHP, JavaScript(브라우저), Bash
  혼합형      : 컴파일러 + 인터프리터 (예: Java → 바이트코드 → JVM)
                예) Java, Kotlin, C#, Python(.pyc)
  크로스 컴파일러 : 다른 기계용 기계어로 번역
                예) GCC(ARM용), Android NDK, iOS 빌드

컴파일러 vs 인터프리터
  컴파일러  : 빠름, 메모리 많이 필요, 목적 코드 재사용 가능
  인터프리터: 느림, 메모리 적게 필요, 매번 다시 번역

언어 처리 시스템 흐름
  소스 프로그램 → 전처리기 → 컴파일러
  → 재배치 가능 기계 코드 → 링커 → 로더
  → 실행 가능한 목적 기계 코드

정규 언어 (Type-3) : 토큰 패턴 정의 → 스캐너
문맥 자유 언어 (Type-2) : 문장 구조 정의 → 파서
```

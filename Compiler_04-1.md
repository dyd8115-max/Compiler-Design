# Introduction to Theory of Computation

---



## 1. 문제의 계산 가능성이란?

> **"Is the given problem computable?"**  
> 주어진 문제를 기계(컴퓨터)가 풀 수 있는가?

### 예시 — 그래프 연결성 문제

```
G = (V, E),  V = {1, 2, 3, 4},  E = {{1,2}, {1,3}, {2,3}, {3,4}}
```

이 그래프가 **연결 그래프(connected graph)** 인지 판별하는 문제.

### 풀이 알고리즘

1. 모든 vertex를 초기에 **unmarked** 상태로 설정
2. 어떤 vertex에 대해 edge가 있으면 → 해당 vertex를 **mark**
3. **unmarked된 vertex가 없으면** → 연결 그래프

### 핵심 질문

> **"이 알고리즘을 사람 대신 기계가 실행할 수 있는가?"**  
> → Turing machine → Automata

- 문제를 **형식적(formal)** 으로 정의해야 기계가 처리할 수 있음
- 이것이 **형식 언어(Formal Language)** 이론의 출발점

---

## 2. 형식 언어란?

> **A language is a set of strings.**  
> 언어 = 문자열들의 집합

### 세 가지 핵심 개념

| 개념 | 설명 |
|------|------|
| **언어 (Language)** | 알파벳으로부터 생성되는 문자열들의 **부분집합** |
| **문법 (Grammar)** | 언어를 **정의**하는 규칙 |
| **인식기 (Recognizer)** | 언어를 **인식**하는 기계 = Automata |

### 해설

- **알파벳** : 언어를 구성하는 기본 기호의 집합 (ex: `{a, b}`, `{0, 1}`)
- **문자열** : 알파벳으로 만든 기호들의 나열 (ex: `ab`, `aabb`, `ε`)
- **언어** : 그 문자열들 중 문법 규칙을 만족하는 것들의 집합
- **문법** : "어떤 문자열이 이 언어에 속하는가"를 정의
- **인식기** : 문자열을 입력받아 언어에 속하는지 판별하는 기계

```
예: Σ = {a, b} 일 때
언어 L = { ab, aabb, aaabbb, ... }  ← 문법으로 정의됨
인식기(오토마타)가 "aabb"를 읽고 → L에 속하는지 판별
```

---

## 3. 언어의 포함 관계

```
RG ⊂ CFG ⊂ CSG ⊂ UG
```

안쪽으로 갈수록 **더 제한적**, 바깥으로 갈수록 **더 표현력이 강함**.

```
┌──────────────────────────────────────────┐
│  UG (Unrestricted Grammar)               │  ← 제한 없음
│  ┌────────────────────────────────────┐  │
│  │  CSG (Context-Sensitive Grammar)   │  │  ← |LHS| ≤ |RHS|
│  │  ┌──────────────────────────────┐  │  │
│  │  │  CFG (Context-Free Grammar)  │  │  │  ← LHS = Nonterminal 1개
│  │  │  ┌────────────────────────┐  │  │  │
│  │  │  │  RG (Regular Grammar)  │  │  │  │  ← 가장 제한적
│  │  │  └────────────────────────┘  │  │  │
│  │  └──────────────────────────────┘  │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

### 포함 관계 의미

- **RG로 표현 가능한 언어**는 CFG로도 표현 가능
- **CFG로 표현 가능한 언어**는 CSG로도 표현 가능
- 반대 방향은 불가능 (더 안쪽 언어로는 바깥 언어 표현 못함)

---

## 4. 문법의 형식적 정의

> **Grammar G = (T, N, P, S)**,  단 S ∈ N

| 구성 요소 | 의미 | 설명 |
|----------|------|------|
| **T** | Terminals | 단말 기호 집합 (입력 기호, 더 이상 치환 안 됨) |
| **N** | Nonterminals | 비단말 기호 집합 (치환 가능한 기호) |
| **P** | Production rules | 생성 규칙 집합 (LHS → RHS) |
| **S** | Start symbol | 시작 기호 (반드시 S ∈ N) |

### 조건

- `T ∩ N = ∅` : 단말과 비단말은 **서로 겹치지 않음**
- 시작 기호 S는 반드시 **비단말 기호**여야 함

### 해설

```
예: ac 언어의 문법
T = { f, i, p, =, +, -, 변수명, 숫자 }     ← 단말 기호
N = { Prog, Dcls, Dcl, Stmts, Stmt, ... }  ← 비단말 기호
S = Prog                                    ← 시작 기호
P = { Prog → Dcls Stmts $, Dcls → Dcl Dcls | ε, ... }  ← 생성 규칙
```

---

## 5. 문법의 분류 (Classes of Grammars)

> 관례: α, β, γ = 임의의 문자열 / A, B, S = Nonterminal / a, b, c = Terminal

### Type 0 — Unrestricted Grammar (무제약 문법)

- **제약 없음** : 생성 규칙에 아무 제한이 없음
- LHS에 Terminal, Nonterminal 마음대로 올 수 있음

```
예: SaB → cS
    (LHS에 S, a, B 혼합 가능)
```

### Type 1 — Context-Sensitive Grammar (문맥 인식 문법, CSG)

```
αAγ → αβγ,  |LHS| ≤ |RHS|
```

- **문맥(context)에 따라** A를 β로 치환 (α, γ가 문맥)
- **LHS 길이 ≤ RHS 길이** : 규칙 적용 시 문자열이 짧아지지 않음

```
예: S aB → c aB
    (S를 c로 바꾸는데, 앞뒤 문맥 aB가 있을 때만 적용)
```

### Type 2 — Context-Free Grammar (문맥 자유 문법, CFG)

```
A → α
```

- **LHS = Nonterminal 1개만** 올 수 있음
- 문맥에 상관없이 A를 α로 치환 가능 (문맥 자유)
- **프로그래밍 언어 문법**이 여기에 해당

```
예: A → aABb
    (A는 문맥 상관없이 aABb로 치환)
```

> **LHS에 Nonterminal 1개만** 올 수 있다.

### Type 3 — Regular Grammar (정규 문법, RG)

```
A → aB   (Terminal 뒤에 Nonterminal)
A → a    (Terminal만)
```

- **LHS = Nonterminal 1개만**
- **RHS = Terminal 1개 + Nonterminal 최대 1개** (Nonterminal은 오른쪽 끝에)
- 가장 제한적인 문법
- **스캐너(어휘 분석기)** 에서 사용

> **RHS에 Nonterminal 1개만 있으며, 위치는 rightmost(오른쪽 끝)이다.**  
> 또는 Terminal만 올 수 있다.

### 문법별 LHS/RHS 길이 비교

| 문법 | LHS | RHS |
|------|-----|-----|
| RG | `|N| = 1` | `1 ≤ |N∪T| ≤ 2` |
| CFG | `|N| = 1` | `|LHS| ≤ |RHS|` |
| CSG | 임의 | `|LHS| ≤ |RHS|` |
| UG | 제한 없음 | 제한 없음 |

---

## 6. 촘스키 분류 총정리

> **N. Chomsky** 가 1950년대에 정립한 형식 문법 분류 체계.  
> 컴파일러 이론의 수학적 기반이 됨.

| Chomsky 분류 | 문법 | 형식 언어 | 인식기 (Automata) |
|-------------|------|----------|------------------|
| Type 0 | 무제약 문법 (UG) | 재귀 열거 언어 | 튜링 기계 (Turing machine) |
| Type 1 | 문맥 인식 문법 (CSG) | 문맥 인식 언어 | 선형한계 오토마타 (linear-bounded automata) |
| **Type 2** | **문맥 자유 문법 (CFG)** | **문맥 자유 언어** | **푸시다운 오토마타 (push-down automata)** |
| **Type 3** | **정규 문법 (RG)** | **정규 언어** | **유한 오토마타 (finite automata)** |

> **컴파일러에서 사용하는 것** : Type 2 (파서), Type 3 (스캐너)

### 컴파일러와의 연결

| 컴파일러 단계 | 사용하는 문법 | 인식기 |
|-------------|-------------|-------|
| 스캐너 (어휘 분석) | **Type 3 (RG)** | 유한 오토마타 (NFA/DFA) |
| 파서 (구문 분석) | **Type 2 (CFG)** | 푸시다운 오토마타 |

### 핵심 포인트

- **문법** → 언어를 **생성**하는 규칙
- **인식기(오토마타)** → 문자열이 언어에 속하는지 **판별**하는 기계
- 문법이 복잡할수록(바깥 계층) → 더 강력한 인식기가 필요
- RG ↔ 유한 오토마타는 **서로 동치** (같은 표현력)
- CFG ↔ 푸시다운 오토마타는 **서로 동치**

```
정리:
RG   ─── 유한 오토마타(FA) ─── 스캐너
CFG  ─── 푸시다운 오토마타  ─── 파서
CSG  ─── 선형한계 오토마타
UG   ─── 튜링 기계
```
